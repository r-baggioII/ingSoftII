spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/greedy_empresa
    username: juan
    password: greedy
  thymeleaf:
    cache: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
