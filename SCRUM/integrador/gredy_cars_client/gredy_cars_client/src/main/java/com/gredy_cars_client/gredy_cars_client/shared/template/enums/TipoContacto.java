package com.gredy_cars_client.gredy_cars_client.shared.template.enums;

/**
 * Enum local que replica los valores expuestos por la API de Greedy Cars.
 * Se utiliza en los formularios del cliente para mantener consistencia con
 * el backend y así evitar valores no soportados.
 */
public enum TipoContacto {
    PERSONAL,
    LABORAL,
    EMPRESA
}

