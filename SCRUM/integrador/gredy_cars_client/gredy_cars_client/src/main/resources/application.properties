spring.application.name=gredy_cars_client

# URL base del servidor Greedy Cars (API REST) - PRODUCCIÓN
greedy.api.base-url=http://161.153.217.110:18082/greedy_cars/api

# Puerto del cliente (ajustar según configuración de producción)
# Si se accede por context path /gredy_cars_client, usar configuración de Tomcat
server.servlet.context-path=/gredy_cars_client

# Credenciales para autenticación con la API REST del servidor
# Estas credenciales también se usan para el login personalizado
greedy.api.username=admin
greedy.api.password=GreedyAdmin123!

# Configuración por defecto de Spring Security (DESHABILITADA)
# Ahora usamos CustomUserDetailsService y la página de login personalizada
#spring.security.user.name=admin
#spring.security.user.password=GreedyAdmin123!
#spring.security.user.roles=USER
